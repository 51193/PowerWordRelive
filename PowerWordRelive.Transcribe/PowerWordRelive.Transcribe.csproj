<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="..\PowerWordRelive.Infrastructure\PowerWordRelive.Infrastructure.csproj"/>
    </ItemGroup>

    <Target Name="CopyFunasrScript" AfterTargets="Build">
        <Copy SourceFiles="$(RepoRoot)scripts\funasr_server.py" DestinationFolder="$(OutputPath)"/>
    </Target>

    <Target Name="CopyFunasrRequirements" AfterTargets="Build">
        <Copy SourceFiles="$(RepoRoot)scripts\funasr_venv.requirements.txt" DestinationFolder="$(OutputPath)"/>
    </Target>

</Project>
